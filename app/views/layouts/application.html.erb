<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title><%= content_for?(:title) ? yield(:title) : "Ada's Rideshare Service" %></title>

    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
    <%= favicon_link_tag asset_path('favicon.ico') %>
    <%= csrf_meta_tags %>
</head>

<body>
  <header>
  <!--Navbar-->
<nav class="mb-1 navbar navbar-expand-lg navbar-dark default-color">

  <!-- Navbar brand -->
  <%= link_to "Ada Rideshare", root_path, class: "navbar-brand" %>

  <!-- Collapsible content -->
  <div class="collapse navbar-collapse" id="rideshareNavContent">
    <!-- Links -->
    <ul class="navbar-nav mr-auto">
      <li class="nav-item dropdown">
        <%= link_to "Drivers", 
          drivers_path, 
          class: "nav-link dropdown-toggle", 
          id: "drivers-link", 
          "data-toggle" => "dropdown", 
          "aria-haspopup" => "true",
          "aria-expanded" => "false"  
        %>
        <div class="dropdown-menu dropdown-primary" aria-labelledby="drivers-link">
          <%= link_to "List Drivers", drivers_path, class: "dropdown-item" %>
          <%= link_to "Add a Driver", new_driver_path, class: "dropdown-item" %>
        </div>
      </li>      
      <li class="nav-item dropdown">
        <%= link_to "Passengers", 
          passengers_path, 
          class: "nav-link dropdown-toggle", 
          id: "passengers-link", 
          "data-toggle" => "dropdown", 
          "aria-haspopup" => "true",
          "aria-expanded" => "false"  
        %>
        <div class="dropdown-menu dropdown-primary" aria-labelledby="drivers-link">
          <%= link_to "List Passengers", passengers_path, class: "dropdown-item" %>
          <%= link_to "Add a Passenger", new_passenger_path, class: "dropdown-item" %>
        </div>
      </li>      
    </ul>
    <!-- Links -->
  </div>
  <!-- Collapsible content -->

</nav>
<!--/.Navbar-->
  </header>
  <main>
    <div id="flash-messages">
      <% if flash[:alert].present? %>

      <% end %>

      <!-- Modal -->
      <div class="modal" tabindex="-1" role="dialog">
        <div class="modal-dialog" role="document">
          <div class="modal-content">
            <div class="modal-header">
              <h5 class="modal-title">Modal title</h5>
              <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                <span aria-hidden="true">&times;</span>
              </button>
            </div>
          </div>
        </div>
      </div>

    <%= yield %>
  </main>
  <footer>

  </footer>
</body>
</html>
